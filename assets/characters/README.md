# Character sprites

Placeholder location for character art. Stage 1 uses emoji rendered in the DOM
(see `src/renderer/components/Character.tsx`), so no image files are required yet.

Drop sprite sheets / per-state PNGs here (e.g. `cat-idle.png`, `cat-happy.png`)
and load them from the `Character` component when real art is ready.
